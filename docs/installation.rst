Installation
============

You can install modgen2d via pip:

.. code-block:: bash

   pip install modgen2d

Or from source:

.. code-block:: bash

   git clone https://github.com/geoimaging/modgen2d
   cd modgen2d
   pip install .
